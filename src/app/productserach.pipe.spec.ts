import { ProductserachPipe } from './productserach.pipe';

describe('ProductserachPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductserachPipe();
    expect(pipe).toBeTruthy();
  });
});
